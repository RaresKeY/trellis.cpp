name: CI

on:
  push:
    branches:
      - main
      - 'agent/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hygiene:
    runs-on: ubuntu-24.04
    timeout-minutes: 5

    steps:
      - name: Check out full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check changed-file whitespace
        shell: bash
        env:
          PR_BASE: ${{ github.event.pull_request.base.sha }}
          PUSH_BASE: ${{ github.event.before }}
        run: |
          set -euo pipefail
          base="$PR_BASE"
          if [[ -z "$base" ]]; then
            base="$PUSH_BASE"
          fi
          if [[ -z "$base" || "$base" =~ ^0+$ ]] || ! git cat-file -e "$base^{commit}"; then
            base="$(git rev-parse HEAD^ 2>/dev/null || git rev-list --max-parents=0 HEAD)"
          fi
          git diff --check "$base" "$GITHUB_SHA"

      - name: Check shell syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' script; do
            bash -n "$script"
          done < <(git ls-files -z '*.sh')

      - name: Check third-party notice coverage
        shell: bash
        run: |
          set -euo pipefail
          test -s LICENSE
          test -s THIRD_PARTY_NOTICES.md
          test -s thirdparty/cpp-httplib/LICENSE
          grep -Fq 'Copyright (c) 2025 Jianfeng XIANG' THIRD_PARTY_NOTICES.md
          grep -Fq 'Copyright (C) 2000, Intel Corporation' THIRD_PARTY_NOTICES.md
          grep -Fq 'Copyright (c) 2010, Google Inc.' THIRD_PARTY_NOTICES.md
          grep -Fq 'cp LICENSE THIRD_PARTY_NOTICES.md dist/' .github/workflows/release.yml

      - name: Resolve pinned CUDA license inputs
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --location --retry 3 \
            https://developer.download.nvidia.com/compute/cuda/redist/cuda_cudart/LICENSE.txt \
            --output /tmp/cuda-cudart-LICENSE.txt
          curl --fail --silent --show-error --location --retry 3 \
            https://developer.download.nvidia.com/compute/cuda/redist/libcublas/LICENSE.txt \
            --output /tmp/libcublas-LICENSE.txt
          test -s /tmp/cuda-cudart-LICENSE.txt
          test -s /tmp/libcublas-LICENSE.txt
          sha256sum /tmp/cuda-cudart-LICENSE.txt /tmp/libcublas-LICENSE.txt

      - name: Test GLB topology diagnostics
        run: >
          python3 -m unittest discover
          -s tests
          -p 'test_glb_topology.py'
          -v

  cpu-build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out source and submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends ninja-build

      - name: Configure CPU build
        run: >
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DGGML_OPENMP=OFF
          -DGGML_NATIVE=OFF
          -DTRELLIS_WEBP=OFF

      - name: Build public entry points
        run: >
          cmake --build build
          --target trellis-cli trellis-server post-replay trellis-test-args
          --parallel 2

      - name: Smoke-test argument parsing
        run: |
          ./build/trellis-cli --help
          ./build/trellis-server --help
          ./build/post-replay --help
          ctest --test-dir build --output-on-failure -R trellis-args
          set +e
          ./build/post-replay ignored.bin ignored.glb --webp invalid
          status=$?
          set -e
          test "$status" -eq 2
          set +e
          ./build/post-replay ignored.bin ignored.glb --meshopt --decim 0
          status=$?
          set -e
          test "$status" -eq 2
